# Student Account System - Architecture Diagram

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          BOOKING FLOW                                    │
└─────────────────────────────────────────────────────────────────────────┘

    Student                Frontend               Backend              Database
       │                      │                      │                    │
       │  1. Book Interview   │                      │                    │
       ├─────────────────────>│                      │                    │
       │                      │  2. Create Payment   │                    │
       │                      ├─────────────────────>│                    │
       │                      │                      │  3. Check User     │
       │                      │                      ├───────────────────>│
       │                      │                      │<───────────────────┤
       │                      │                      │  User exists?      │
       │                      │                      │                    │
       │                      │                      │  4. Create Profile │
       │                      │                      ├───────────────────>│
       │                      │                      │<───────────────────┤
       │                      │                      │  Profile created   │
       │                      │                      │                    │
       │  5. Confirmation     │                      │                    │
       │  Email with Dashboard│                      │                    │
       │<─────────────────────┼──────────────────────┤                    │
       │  Link                │                      │                    │
       │                      │                      │                    │

┌─────────────────────────────────────────────────────────────────────────┐
│                        DASHBOARD ACCESS                                  │
└─────────────────────────────────────────────────────────────────────────┘

    Student                Frontend               Backend              Database
       │                      │                      │                    │
       │  1. Click Link       │                      │                    │
       ├─────────────────────>│                      │                    │
       │  (with student_id)   │                      │                    │
       │                      │                      │                    │
       │                      │  2. Load Profile     │                    │
       │                      ├─────────────────────>│                    │
       │                      │                      │  3. Query Profile  │
       │                      │                      ├───────────────────>│
       │                      │                      │<───────────────────┤
       │                      │<─────────────────────┤  Profile data      │
       │                      │  Profile data        │                    │
       │                      │                      │                    │
       │                      │  4. Load Sessions    │                    │
       │                      ├─────────────────────>│                    │
       │                      │                      │  5. Query Sessions │
       │                      │                      ├───────────────────>│
       │                      │                      │<───────────────────┤
       │  6. Dashboard        │<─────────────────────┤  Sessions data     │
       │  Rendered            │  Sessions data       │                    │
       │<─────────────────────┤                      │                    │
       │                      │                      │                    │

┌─────────────────────────────────────────────────────────────────────────┐
│                   AVAILABILITY SUBMISSION                                │
└─────────────────────────────────────────────────────────────────────────┘

    Student                Frontend               Backend              Database
       │                      │                      │                    │
       │  1. Fill Form        │                      │                    │
       │  (dates, times)      │                      │                    │
       ├─────────────────────>│                      │                    │
       │                      │                      │                    │
       │  2. Submit           │                      │                    │
       ├─────────────────────>│                      │                    │
       │                      │  3. POST Request     │                    │
       │                      ├─────────────────────>│                    │
       │                      │                      │  4. Validate       │
       │                      │                      │                    │
       │                      │                      │  5. Upsert Profile │
       │                      │                      ├───────────────────>│
       │                      │                      │<───────────────────┤
       │                      │                      │  Profile updated   │
       │                      │                      │                    │
       │                      │                      │  6. Insert Slots   │
       │                      │                      ├───────────────────>│
       │                      │                      │<───────────────────┤
       │  7. Success          │<─────────────────────┤  Slots saved       │
       │  Message             │  Success response    │                    │
       │<─────────────────────┤                      │                    │
       │                      │                      │                    │
```

## Database Schema Relationships

```
┌──────────────────────┐
│      bookings        │
│──────────────────────│
│ id (PK)              │───┐
│ user_id (FK)         │   │
│ email                │   │
│ package              │   │
│ amount               │   │
│ payment_status       │   │
│ ...                  │   │
└──────────────────────┘   │
                           │
                           │  created_from_booking_id
                           │
┌──────────────────────┐   │         ┌──────────────────────┐
│       users          │   │         │  student_profiles    │
│──────────────────────│   │         │──────────────────────│
│ id (PK)              │───┼────────>│ user_id (FK)         │
│ email (UNIQUE)       │   │         │ auth_id              │
│ full_name            │   └────────>│ created_from_booking │
│ role                 │             │ timezone             │
│ phone_number         │             │ weekly_availability  │
│ stripe_customer_id   │             │ preferences          │
│ created_at           │             │ ...                  │
└──────────────────────┘             └──────────────────────┘
          │
          │
          │  student_id                    ┌──────────────────────┐
          ├───────────────────────────────>│ student_availability │
          │                                │──────────────────────│
          │                                │ id (PK)              │
          │                                │ student_id (FK)      │
          │                                │ date                 │
          │                                │ hour_start           │
          │                                │ hour_end             │
          │                                │ type                 │
          │                                │ ...                  │
          │                                └──────────────────────┘
          │
          │  student_id                    ┌──────────────────────┐
          └───────────────────────────────>│     interviews       │
                                           │──────────────────────│
                                           │ id (PK)              │
                                           │ student_id (FK)      │
                                           │ tutor_id (FK)        │
                  ┌────────────────────────│ booking_id (FK)      │
                  │                        │ university           │
                  │                        │ scheduled_at         │
                  │                        │ status               │
                  │                        │ zoom_join_url        │
                  │                        │ completed            │
                  │                        │ ...                  │
                  │                        └──────────────────────┘
                  │                                │
                  │                                │  tutor_id
                  │                                │
                  │                                ▼
                  │                        ┌──────────────────────┐
                  │                        │       tutors         │
                  │                        │──────────────────────│
                  │                        │ id (PK)              │
                  │                        │ name                 │
                  │                        │ email                │
                  │                        │ subjects             │
                  │                        │ ...                  │
                  │                        └──────────────────────┘
                  │
                  │  booking_id
                  │
                  └──────────(back to bookings)
```

## Component Hierarchy

```
app/student-dashboard/page.tsx
│
├─> StudentProvider (Context)
│   │
│   ├─> State:
│   │   ├─ studentId
│   │   ├─ profile
│   │   ├─ sessions
│   │   ├─ upcomingSessions (computed)
│   │   ├─ previousSessions (computed)
│   │   └─ loading, error
│   │
│   └─> Functions:
│       ├─ refreshProfile()
│       ├─ refreshSessions()
│       └─ submitAvailability()
│
└─> StudentDashboardContent
    │
    ├─> Header
    │   └─ Display: user name, email
    │
    └─> Main Content
        │
        ├─> Tabs (sessions | availability)
        │
        ├─> [Sessions Tab]
        │   │
        │   └─> SessionsList
        │       │
        │       ├─> Filter Buttons (upcoming | previous)
        │       │
        │       └─> SessionCard (for each session)
        │           ├─ University name
        │           ├─ Date & time
        │           ├─ Tutor info
        │           ├─ Status badge
        │           ├─ Notes
        │           └─ Action buttons (Join Zoom | Feedback)
        │
        └─> [Availability Tab]
            │
            └─> AvailabilityForm
                │
                ├─> Timezone selector
                ├─> Date picker + time range
                ├─> Added slots display
                ├─> Notes textarea
                └─> Submit button
```

## API Endpoints

```
Backend API (http://localhost:5000)
│
├─> POST /api/v1/student/availability
│   │
│   ├─ Body: {
│   │   student_id: string,
│   │   timezone?: string,
│   │   specific_dates?: [{
│   │     date: string,
│   │     hour_start: number,
│   │     hour_end: number,
│   │     type?: 'interview' | 'tutoring' | 'consultation'
│   │   }],
│   │   notes?: string
│   │ }
│   │
│   └─ Response: {
│       success: boolean,
│       data: { profile, availability }
│     }
│
├─> GET /api/v1/student/sessions
│   │
│   ├─ Query: student_id, filter? (upcoming | previous)
│   │
│   └─ Response: {
│       success: boolean,
│       data: Session[]
│     }
│
└─> GET /api/v1/student/profile
    │
    ├─ Query: student_id
    │
    └─ Response: {
        success: boolean,
        data: {
          id, user_id, timezone, 
          weekly_availability, preferences,
          users: { email, full_name, phone_number }
        }
      }
```

## Email Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    Booking Confirmation Email                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Subject: Interview Preparation Booking Confirmed           │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Hi [Student Name],                                   │  │
│  │                                                        │  │
│  │  Thank you for booking Interview Preparation! 🎉      │  │
│  │                                                        │  │
│  │  [Booking Details Box]                                │  │
│  │    - Booking ID                                       │  │
│  │    - Package & Service                                │  │
│  │    - Universities                                     │  │
│  │    - Amount                                           │  │
│  │                                                        │  │
│  │  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │  │
│  │  ┃    🎓 YOUR STUDENT DASHBOARD                    ┃  │  │
│  │  ┃                                                  ┃  │  │
│  │  ┃  • Submit your availability                     ┃  │  │
│  │  ┃  • View upcoming sessions                       ┃  │  │
│  │  ┃  • Access materials & Zoom links                ┃  │  │
│  │  ┃  • Track your progress                          ┃  │  │
│  │  ┃                                                  ┃  │  │
│  │  ┃    [ Access Your Dashboard → ]                  ┃  │  │
│  │  ┃    (Blue CTA Button)                            ┃  │  │
│  │  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │  │
│  │                                                        │  │
│  │  [What Happens Next Box]                              │  │
│  │    • Team reviews your details                        │  │
│  │    • Contact within 24 hours                          │  │
│  │    • Preparation materials sent                       │  │
│  │                                                        │  │
│  │  Best regards,                                         │  │
│  │  The NextGen MedPrep Team                             │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  Link includes: ?student_id=[UUID]                          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## State Management Flow

```
StudentContext
│
├─> On Mount (when studentId set)
│   │
│   ├─ Fetch Profile
│   │   └─> GET /api/v1/student/profile?student_id=...
│   │       └─> Update profile state
│   │
│   └─ Fetch Sessions
│       └─> GET /api/v1/student/sessions?student_id=...
│           └─> Update sessions state
│
├─> Computed Properties
│   │
│   ├─ upcomingSessions
│   │   └─> Filter: scheduled_at >= now && !completed
│   │
│   └─ previousSessions
│       └─> Filter: completed || scheduled_at < now
│
└─> Actions
    │
    ├─ submitAvailability(data)
    │   └─> POST /api/v1/student/availability
    │       └─> refreshProfile()
    │
    ├─ refreshProfile()
    │   └─> GET /api/v1/student/profile
    │
    └─ refreshSessions()
        └─> GET /api/v1/student/sessions
```

---

This visual guide helps understand the complete system architecture, data flow, and component relationships.
